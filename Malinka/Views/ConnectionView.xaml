<UserControl 
    x:Class="Malinka.Views.ConnectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:prism="http://prismlibrary.com/"
    xmlns:material="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:res="clr-namespace:Malinka.Lang.Properties;assembly=Malinka.Lang"
    prism:ViewModelLocator.AutoWireViewModel="True">

    <material:ColorZone Style="{StaticResource CornerZone}">
        <StackPanel Margin="25"
                    Style="{StaticResource CenterStack}">

            <TextBlock Text="{x:Static res:Resources.Connecting}"
                       Style="{StaticResource TitleBlock}"/>

            <ProgressBar Height="50"
                         Width="50"
                         IsIndeterminate="True"
                         Style="{StaticResource MaterialDesignCircularProgressBar}"/>
        </StackPanel>
    </material:ColorZone>
</UserControl>
